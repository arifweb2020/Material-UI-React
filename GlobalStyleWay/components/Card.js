import React from 'react';
import { HeadingColor } from '../GlobalStyle';

function Card(props) {
    return (
        <div>
            <HeadingColor>components</HeadingColor>
        </div>
    );
}

export default Card;